# Copy to .env (which is gitignored) and fill in real values.
# NEVER commit .env or any file containing these values.

# MiniMax API (for resume rewriting)
MINIMAX_API_KEY=sk-replace-me
MINIMAX_BASE_URL=https://api.minimax.io/v1
MINIMAX_MODEL=minimax-m2

# Stripe
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me
STRIPE_PRICE_PRO=price_replace_me_lifetime
STRIPE_PRICE_STUDIO_LIFETIME=price_replace_me_lifetime
STRIPE_PRICE_STUDIO_MONTHLY=price_replace_me_monthly

# Supabase
SUPABASE_URL=https://replace-me.supabase.co
SUPABASE_ANON_KEY=eyJ_replace_me
SUPABASE_SERVICE_ROLE_KEY=eyJ_replace_me_service_role

# Resend (transactional email)
RESEND_API_KEY=re_replace_me

# App
APP_URL=https://hiremekit.app
NODE_ENV=development

# Encryption key for PII at rest (32-byte hex)
PII_ENCRYPTION_KEY=replace_me_with_64_hex_chars_replace_me_with_64_hex_chars
